@page "/password"
<RadzenLabel Text="Passwords" />
@foreach (var password in this.Passwords)
{
    <RadzenRow>
        <RadzenColumn Size="6">
            <RadzenLabel Text="@password.ID.ToString()" />
        </RadzenColumn>
        <RadzenColumn Size="6">
            <RadzenLabel Text="@password.Name" />
        </RadzenColumn>
    </RadzenRow>
}